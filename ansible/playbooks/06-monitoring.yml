---
# 06-monitoring.yml (placeholder)
# This playbook will be implemented after scope is finalized.
# Secrets must be sourced from ansible/vault.yml and executed with --ask-vault-pass.

- name: 06-monitoring placeholder
  hosts: all
  become: true
  tasks:
    - name: Placeholder
      ansible.builtin.debug:
        msg: "06-monitoring not implemented yet"
