# Ansible Roles

- `leadgen-host-bootstrap/` — Installs host prerequisites (Podman, SELinux boolean for nginx proxying, directories, Quadlet unit scaffolding).

> Note: This repo intentionally does **not** deploy into `/var/www`. WordPress/LEMP remains isolated.
