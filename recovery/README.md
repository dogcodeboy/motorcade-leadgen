# Recovery Kit

Local-first backup and (later) recovery tooling.

Phase 1:
- local filesystem backups + verify
- portable bundle output

Phase 2 (later):
- remote backup/restore with `.pem` + server selection UI
